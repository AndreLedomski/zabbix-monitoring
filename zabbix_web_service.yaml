apiVersion: v1
kind: Service
metadata:
  name: zabbix-web
  namespace: zabbix
spec:
  type: LoadBalancer  # или NodePort для тестирования
  selector:
    app: zabbix-web
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP

